export default function LeaderboardPage() {
  return (
    <main className="main">
      <h1>Leaderboard Skeleton</h1>
      <p className="lede">
        Leaderboards are partitioned by model, agent, harness, task set, and
        budget. Incompatible runs are not mixed.
      </p>
    </main>
  );
}
