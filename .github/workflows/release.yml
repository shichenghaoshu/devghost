name: Release

on:
  workflow_dispatch:

jobs:
  release-skeleton:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo "Release packaging, SBOM, checksums, and provenance are planned for v0.1 hardening."
