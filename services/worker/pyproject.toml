[project]
name = "ghostbench-worker"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "ghostbench-core",
  "pydantic>=2.12.0"
]

[tool.uv.sources]
ghostbench-core = { workspace = true }

[tool.uv]
package = true
