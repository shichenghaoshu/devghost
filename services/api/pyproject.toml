[project]
name = "ghostbench-api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.124.0",
  "ghostbench-core",
  "pydantic>=2.12.0",
  "uvicorn>=0.38.0"
]

[tool.uv.sources]
ghostbench-core = { workspace = true }

[tool.uv]
package = true
